import * as vscode from 'vscode';
import * as firebase from 'firebase';

export default function takeover(streamId, userId, streamContent): Promise < {} > {

	const promise = new Promise((resolve, reject) => {




		resolve({});
	})

	return promise;
}